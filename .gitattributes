# Auto detect text files and handle line endings properly
* text=auto

# Shell scripts should always have LF line endings
*.sh text eol=lf

# Batch files should always have CRLF line endings  
*.bat text eol=crlf
